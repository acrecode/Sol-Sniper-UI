import React from 'react';
import { createRoot } from 'react-dom/client';
import SolSniper from './SolSniper.jsx';

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SolSniper />
  </React.StrictMode>,
);
